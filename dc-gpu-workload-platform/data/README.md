# data/

Local cache only — never committed. Mirrors the S3 layer structure:

- `raw/` — untouched downloaded/replayed trace data
- `interim/` — cleaned but not yet feature-engineered
- `processed/` — model-ready features
